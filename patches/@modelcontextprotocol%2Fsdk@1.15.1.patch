diff --git a/dist/cjs/client/auth.d.ts b/dist/cjs/client/auth.d.ts
index 543964d2735cd930948c6ad808d82f4e8dd3a661..fca4081026feea11c04104381a0efcacbc4139b9 100644
--- a/dist/cjs/client/auth.d.ts
+++ b/dist/cjs/client/auth.d.ts
@@ -102,6 +102,12 @@ export declare function auth(provider: OAuthClientProvider, { serverUrl, authori
     scope?: string;
     resourceMetadataUrl?: URL;
 }): Promise<AuthResult>;
+export declare function determineScope(options: {
+    requestedScope?: string;
+    resourceMetadata?: OAuthProtectedResourceMetadata;
+    authServerMetadata?: OAuthMetadata;
+    clientMetadata: OAuthClientMetadata;
+}): string | undefined;
 export declare function selectResourceURL(serverUrl: string | URL, provider: OAuthClientProvider, resourceMetadata?: OAuthProtectedResourceMetadata): Promise<URL | undefined>;
 /**
  * Extract resource_metadata from response header.
diff --git a/dist/cjs/client/auth.js b/dist/cjs/client/auth.js
index 127ada5619e693e5cf6d50d5290599527f80f5e5..fa96bab8d077996217d5bc4e13029d50b00070de 100644
--- a/dist/cjs/client/auth.js
+++ b/dist/cjs/client/auth.js
@@ -5,6 +5,7 @@ var __importDefault = (this && this.__importDefault) || function (mod) {
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.UnauthorizedError = void 0;
 exports.auth = auth;
+exports.determineScope = determineScope;
 exports.selectResourceURL = selectResourceURL;
 exports.extractResourceMetadataUrl = extractResourceMetadataUrl;
 exports.discoverOAuthProtectedResourceMetadata = discoverOAuthProtectedResourceMetadata;
@@ -181,6 +182,12 @@ async function auth(provider, { serverUrl, authorizationCode, scope, resourceMet
             // Could not refresh OAuth tokens
         }
     }
+    const resolvedScope = determineScope({
+        requestedScope: scope,
+        resourceMetadata,
+        authServerMetadata: metadata,
+        clientMetadata: provider.clientMetadata
+    });
     const state = provider.state ? await provider.state() : undefined;
     // Start new authorization flow
     const { authorizationUrl, codeVerifier } = await startAuthorization(authorizationServerUrl, {
@@ -188,13 +195,21 @@ async function auth(provider, { serverUrl, authorizationCode, scope, resourceMet
         clientInformation,
         state,
         redirectUrl: provider.redirectUrl,
-        scope: scope || provider.clientMetadata.scope,
+        scope: resolvedScope,
         resource,
     });
     await provider.saveCodeVerifier(codeVerifier);
     await provider.redirectToAuthorization(authorizationUrl);
     return "REDIRECT";
 }
+function determineScope({ requestedScope, resourceMetadata, authServerMetadata, clientMetadata }) {
+    let scope = requestedScope || resourceMetadata?.scopes_supported?.join(" ") || clientMetadata.scope;
+    if (scope && authServerMetadata?.scopes_supported?.includes("offline_access") &&
+        !scope.split(" ").includes("offline_access") && clientMetadata.grant_types?.includes("refresh_token")) {
+        scope += " offline_access";
+    }
+    return scope;
+}
 async function selectResourceURL(serverUrl, provider, resourceMetadata) {
     const defaultResource = (0, auth_utils_js_1.resourceUrlFromServerUrl)(serverUrl);
     // If provider has custom validation, delegate to it
diff --git a/dist/esm/client/auth.d.ts b/dist/esm/client/auth.d.ts
index 543964d2735cd930948c6ad808d82f4e8dd3a661..fca4081026feea11c04104381a0efcacbc4139b9 100644
--- a/dist/esm/client/auth.d.ts
+++ b/dist/esm/client/auth.d.ts
@@ -102,6 +102,12 @@ export declare function auth(provider: OAuthClientProvider, { serverUrl, authori
     scope?: string;
     resourceMetadataUrl?: URL;
 }): Promise<AuthResult>;
+export declare function determineScope(options: {
+    requestedScope?: string;
+    resourceMetadata?: OAuthProtectedResourceMetadata;
+    authServerMetadata?: OAuthMetadata;
+    clientMetadata: OAuthClientMetadata;
+}): string | undefined;
 export declare function selectResourceURL(serverUrl: string | URL, provider: OAuthClientProvider, resourceMetadata?: OAuthProtectedResourceMetadata): Promise<URL | undefined>;
 /**
  * Extract resource_metadata from response header.
diff --git a/dist/esm/client/auth.js b/dist/esm/client/auth.js
index 11fd9025ecd90080deca3bcc4f6669bc85dd30d9..0d739eff869a2268ac96ff32317450f4784d986a 100644
--- a/dist/esm/client/auth.js
+++ b/dist/esm/client/auth.js
@@ -165,6 +165,12 @@ export async function auth(provider, { serverUrl, authorizationCode, scope, reso
             // Could not refresh OAuth tokens
         }
     }
+    const resolvedScope = determineScope({
+        requestedScope: scope,
+        resourceMetadata,
+        authServerMetadata: metadata,
+        clientMetadata: provider.clientMetadata
+    });
     const state = provider.state ? await provider.state() : undefined;
     // Start new authorization flow
     const { authorizationUrl, codeVerifier } = await startAuthorization(authorizationServerUrl, {
@@ -172,13 +178,21 @@ export async function auth(provider, { serverUrl, authorizationCode, scope, reso
         clientInformation,
         state,
         redirectUrl: provider.redirectUrl,
-        scope: scope || provider.clientMetadata.scope,
+        scope: resolvedScope,
         resource,
     });
     await provider.saveCodeVerifier(codeVerifier);
     await provider.redirectToAuthorization(authorizationUrl);
     return "REDIRECT";
 }
+export function determineScope({ requestedScope, resourceMetadata, authServerMetadata, clientMetadata }) {
+    let scope = requestedScope || resourceMetadata?.scopes_supported?.join(" ") || clientMetadata.scope;
+    if (scope && authServerMetadata?.scopes_supported?.includes("offline_access") &&
+        !scope.split(" ").includes("offline_access") && clientMetadata.grant_types?.includes("refresh_token")) {
+        scope += " offline_access";
+    }
+    return scope;
+}
 export async function selectResourceURL(serverUrl, provider, resourceMetadata) {
     const defaultResource = resourceUrlFromServerUrl(serverUrl);
     // If provider has custom validation, delegate to it
