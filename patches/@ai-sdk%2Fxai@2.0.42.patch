diff --git a/dist/index.d.mts b/dist/index.d.mts
index f0043e7c114240ddcec63cf521dc60c7c56d21fc..0fa307ab9a20d6f360cda59decb2f3ec521b24b8 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -68,6 +68,7 @@ declare const xaiResponsesProviderOptions: z.ZodObject<{
     }>>;
     store: z.ZodOptional<z.ZodBoolean>;
     previousResponseId: z.ZodOptional<z.ZodString>;
+    promptCacheKey: z.ZodOptional<z.ZodString>;
 }, z.core.$strip>;
 type XaiResponsesProviderOptions = z.infer<typeof xaiResponsesProviderOptions>;
 
diff --git a/dist/index.d.ts b/dist/index.d.ts
index f0043e7c114240ddcec63cf521dc60c7c56d21fc..0fa307ab9a20d6f360cda59decb2f3ec521b24b8 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -68,6 +68,7 @@ declare const xaiResponsesProviderOptions: z.ZodObject<{
     }>>;
     store: z.ZodOptional<z.ZodBoolean>;
     previousResponseId: z.ZodOptional<z.ZodString>;
+    promptCacheKey: z.ZodOptional<z.ZodString>;
 }, z.core.$strip>;
 type XaiResponsesProviderOptions = z.infer<typeof xaiResponsesProviderOptions>;
 
diff --git a/dist/index.js b/dist/index.js
index 36002d5edabec3c7a630270465f1c1d1447e99cb..5b675b0f9d718be9cbc3d9ee858c7856c5bfedf7 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1073,7 +1073,8 @@ var xaiResponsesProviderOptions = import_v45.z.object({
   /**
    * The ID of the previous response from the model.
    */
-  previousResponseId: import_v45.z.string().optional()
+  previousResponseId: import_v45.z.string().optional(),
+  promptCacheKey: import_v45.z.string().optional()
 });
 
 // src/responses/convert-to-xai-responses-input.ts
@@ -1093,18 +1094,28 @@ async function convertToXaiResponsesInput({
         break;
       }
       case "user": {
-        let userContent = "";
+        const userContent = [];
         for (const block of message.content) {
           switch (block.type) {
             case "text": {
-              userContent += block.text;
+              userContent.push({ type: "input_text", text: block.text });
               break;
             }
             case "file": {
-              inputWarnings.push({
-                type: "other",
-                message: `xAI Responses API does not support ${block.type} in user messages`
-              });
+              if (block.mediaType === "application/pdf") {
+                userContent.push({
+                  type: "input_file",
+                  ...block.data instanceof URL ? { file_url: block.data.toString() } : typeof block.data === "string" && block.data.startsWith("file-") ? { file_id: block.data } : {
+                    filename: block.filename ?? "file.pdf",
+                    file_data: `data:application/pdf;base64,${(0, import_provider_utils.convertToBase64)(block.data)}`
+                  }
+                });
+              } else {
+                inputWarnings.push({
+                  type: "other",
+                  message: `xAI Responses API does not support ${block.mediaType} files in user messages`
+                });
+              }
               break;
             }
             default: {
@@ -1543,6 +1554,9 @@ var XaiResponsesLanguageModel = class {
       },
       ...options.previousResponseId != null && {
         previous_response_id: options.previousResponseId
+      },
+      ...options.promptCacheKey != null && {
+        prompt_cache_key: options.promptCacheKey
       }
     };
     if (xaiTools2 && xaiTools2.length > 0) {
diff --git a/dist/index.mjs b/dist/index.mjs
index 202eb6c9a56c7dd1845c23ce47f75d96fb2dc1f2..f969858fa0f4d8db2363d4ad534d1254f7fbf503 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1064,7 +1064,8 @@ var xaiResponsesProviderOptions = z5.object({
   /**
    * The ID of the previous response from the model.
    */
-  previousResponseId: z5.string().optional()
+  previousResponseId: z5.string().optional(),
+  promptCacheKey: z5.string().optional()
 });
 
 // src/responses/convert-to-xai-responses-input.ts
@@ -1084,18 +1085,28 @@ async function convertToXaiResponsesInput({
         break;
       }
       case "user": {
-        let userContent = "";
+        const userContent = [];
         for (const block of message.content) {
           switch (block.type) {
             case "text": {
-              userContent += block.text;
+              userContent.push({ type: "input_text", text: block.text });
               break;
             }
             case "file": {
-              inputWarnings.push({
-                type: "other",
-                message: `xAI Responses API does not support ${block.type} in user messages`
-              });
+              if (block.mediaType === "application/pdf") {
+                userContent.push({
+                  type: "input_file",
+                  ...block.data instanceof URL ? { file_url: block.data.toString() } : typeof block.data === "string" && block.data.startsWith("file-") ? { file_id: block.data } : {
+                    filename: block.filename ?? "file.pdf",
+                    file_data: `data:application/pdf;base64,${convertToBase64(block.data)}`
+                  }
+                });
+              } else {
+                inputWarnings.push({
+                  type: "other",
+                  message: `xAI Responses API does not support ${block.mediaType} files in user messages`
+                });
+              }
               break;
             }
             default: {
@@ -1544,6 +1555,9 @@ var XaiResponsesLanguageModel = class {
       },
       ...options.previousResponseId != null && {
         previous_response_id: options.previousResponseId
+      },
+      ...options.promptCacheKey != null && {
+        prompt_cache_key: options.promptCacheKey
       }
     };
     if (xaiTools2 && xaiTools2.length > 0) {
