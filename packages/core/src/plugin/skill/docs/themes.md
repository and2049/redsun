# Themes

Themes are currently a terminal-client capability rather than a shared redsun configuration feature. The terminal
client includes built-in light and dark themes, supports terminal-aware color modes, and owns theme selection and custom
theme loading.

See [CLI config](cli/config.md) for terminal-client theme settings.
